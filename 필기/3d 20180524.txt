디바이스 -> 크리에이트 함수 호출시 바로 실행됨
커맨드 리스트 -> 바로 실행되는것이 아님

리소스의 타입에 따라서 어떤 힙에 생성할 건지 정해줘야함
GPU ---> UPLOAD HEAP <--- CPU
	             write

GPU인스턴싱
GPU <---------------------------------- CPU
        모든 정보를 한 버퍼에 넣어서
        한번에 처리

정점 데이터는 위치정보가 없음
(x, y, z, w) w가 0이면 방향벡터
	     w가 1이면 위치벡터(변환에 적용하기 위해)

리소스들은 뷰를 통해서 파이프라인과 연결이 됌


과제 2(3주)

다렉 12로 바꾸기
육면체 각 면의 색깔이 다름
픽킹한 면의 색깔로 터져야함
